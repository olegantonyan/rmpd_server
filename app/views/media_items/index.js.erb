<% js = escape_javascript(
  render(partial: 'list', locals: {media_items: @media_items})
) %>
$("#filterrific_results").html("<%= js %>");

if(typeof window.setup_select_all === 'function') {
    window.setup_select_all();
}
